# 整体风格学习与二次创作方案

## 🎯 核心目标

从原动漫素材中**学习整体风格**（而非单张图片参考），然后基于学习的风格生成符合解说场景的新图片和视频。

## 🚀 方案对比

### 方案A：单张图片参考（当前）
- ❌ 只能参考单张图片的风格
- ❌ 风格学习不全面
- ❌ 不同场景风格可能不一致

### 方案B：整体风格学习（推荐）⭐

**核心思路：**
```
原动漫素材（多张图片/视频）
  ↓ 提取关键帧（20-50张）
  ↓ 训练风格LoRA（学习整体风格）
风格LoRA模型
  ↓ 结合InstantID + 角色LoRA
生成新图片（保持整体风格）
  ↓ SVD
生成新视频
```

**优点：**
- ✅ 学习整体风格，而非单张图片
- ✅ 风格统一，所有场景保持一致
- ✅ 可以生成任意场景，都保持原动漫风格
- ✅ 训练一次，长期使用

## 🛠️ 技术实现方案

### 方案1：风格LoRA训练（最推荐）⭐⭐⭐

**流程：**

1. **提取风格参考图像**
   ```bash
   # 从原动漫视频中提取20-50张关键帧
   python tools/extract_style_reference_frames.py \
       --input reference_materials/videos/ \
       --output reference_materials/style_frames/ \
       --num_frames 30 \
       --method diverse  # 确保多样性
   ```

2. **训练风格LoRA**
   ```bash
   # 使用DreamBooth LoRA训练风格模型
   python scripts/train_style_lora_sdxl.py \
       --instance_data_dir reference_materials/style_frames/ \
       --output_dir models/lora/anime_style/ \
       --instance_prompt "anime style, xianxia animation style" \
       --resolution 1024 \
       --train_batch_size 1 \
       --gradient_accumulation_steps 4 \
       --learning_rate 1e-4 \
       --max_train_steps 1000
   ```

3. **使用风格LoRA生成**
   ```yaml
   # config.yaml
   image:
     lora:
       enabled: true
       # 角色LoRA
       weights_path: models/lora/hanli/pytorch_lora_weights.safetensors
       adapter_name: hanli
       alpha: 0.60
       # 风格LoRA（新增）
       style_lora:
         enabled: true
         weights_path: models/lora/anime_style/pytorch_lora_weights.safetensors
         adapter_name: anime_style
         alpha: 0.7  # 风格权重，控制风格强度
   ```

**特点：**
- 训练时间：约1-2小时（30张图片，1000步）
- 模型大小：约50-100MB
- 效果：风格统一，质量高
- 可复用：训练一次，所有场景都使用

### 方案2：IP-Adapter Plus 多参考图像 ⭐⭐

**流程：**

1. **准备多参考图像**
   - 从原动漫中提取10-20张代表性图像
   - 覆盖不同场景类型（室内、室外、战斗、对话等）

2. **使用IP-Adapter Plus**
   ```python
   # 支持多参考图像
   from diffusers import StableDiffusionXLControlNetPipeline
   from diffusers.pipelines.controlnet import MultiControlNetModel
   
   # 加载多张参考图像
   reference_images = [
       load_image("style_ref_001.jpg"),
       load_image("style_ref_002.jpg"),
       load_image("style_ref_003.jpg"),
       # ... 更多参考图像
   ]
   
   # 使用IP-Adapter Plus
   pipe.set_ip_adapter_scale(0.6)
   image = pipe(
       prompt=prompt,
       ip_adapter_image=reference_images,  # 多参考图像
       num_inference_steps=40,
   ).images[0]
   ```

**特点：**
- 无需训练，直接使用
- 支持动态切换参考图像
- 效果：风格迁移，但可能不如LoRA统一

### 方案3：Style Embedding提取 ⭐

**流程：**

1. **提取风格特征向量**
   ```python
   # 使用CLIP提取多张参考图像的风格特征
   from transformers import CLIPProcessor, CLIPModel
   
   clip_model = CLIPModel.from_pretrained("openai/clip-vit-large-patch14")
   clip_processor = CLIPProcessor.from_pretrained("openai/clip-vit-large-patch14")
   
   # 提取多张图像的特征
   style_embeddings = []
   for img_path in reference_images:
       image = Image.open(img_path)
       inputs = clip_processor(images=image, return_tensors="pt")
       embedding = clip_model.get_image_features(**inputs)
       style_embeddings.append(embedding)
   
   # 平均风格特征
   avg_style_embedding = torch.mean(torch.stack(style_embeddings), dim=0)
   ```

2. **应用到生成过程**
   - 将风格特征注入到生成过程
   - 结合ControlNet或IP-Adapter使用

**特点：**
- 轻量级，无需训练
- 可以动态调整风格强度
- 效果：中等，不如LoRA稳定

### 方案4：混合方案（最佳）⭐⭐⭐⭐⭐

**组合使用：**
```
风格LoRA（整体风格）
  + 
角色LoRA（角色一致性）
  +
InstantID（面部特征）
  +
IP-Adapter多参考（场景细节）
  =
完美的二次创作
```

**配置示例：**
```yaml
image:
  engine: instantid
  lora:
    enabled: true
    # 角色LoRA
    weights_path: models/lora/hanli/pytorch_lora_weights.safetensors
    adapter_name: hanli
    alpha: 0.60
    # 风格LoRA（新增）
    style_lora:
      enabled: true
      weights_path: models/lora/anime_style/pytorch_lora_weights.safetensors
      adapter_name: anime_style
      alpha: 0.7
  
  # 风格参考配置
  style_reference:
    enabled: true
    # 使用IP-Adapter Plus支持多参考图像
    use_ip_adapter_plus: true
    # 参考图像目录
    reference_dir: reference_materials/style_frames/
    # 每次生成使用的参考图像数量
    num_references: 3
    # 参考图像权重
    reference_scale: 0.5
```

## 📊 方案对比表

| 方案 | 训练时间 | 模型大小 | 风格统一性 | 灵活性 | 推荐度 |
|------|---------|---------|----------|--------|--------|
| 单张参考 | 0 | 0 | ⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| 风格LoRA | 1-2小时 | 50-100MB | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| IP-Adapter Plus | 0 | 0 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Style Embedding | 0 | 小 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| 混合方案 | 1-2小时 | 50-100MB | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

## 🎯 推荐实施方案

### 阶段1：快速验证（1-2天）

使用 **IP-Adapter Plus 多参考图像**：
- 无需训练，快速验证效果
- 提取10-20张代表性参考图像
- 实现多参考图像支持

### 阶段2：优化方案（3-5天）

训练 **风格LoRA**：
- 提取30-50张风格参考图像
- 训练风格LoRA模型
- 集成到现有生成流程

### 阶段3：完善方案（持续优化）

实现 **混合方案**：
- 风格LoRA + IP-Adapter Plus
- 动态调整风格强度
- 场景自适应风格选择

## 🔧 实现细节

### 1. 风格参考图像提取

**要求：**
- 多样性：覆盖不同场景、角度、光照
- 代表性：选择最能体现整体风格的图像
- 数量：20-50张（LoRA训练），10-20张（IP-Adapter）

**提取策略：**
```python
# 1. 场景变化检测：提取场景切换的关键帧
# 2. 多样性采样：确保覆盖不同场景类型
# 3. 质量筛选：选择清晰、有代表性的图像
# 4. 风格一致性检查：确保风格统一
```

### 2. 风格LoRA训练

**训练参数：**
- 学习率：1e-4
- 训练步数：800-1200步
- Batch size：1-2
- 分辨率：1024x1024
- 提示词：`"anime style, xianxia animation style, [原动漫名称] style"`

**训练技巧：**
- 使用高质量参考图像
- 确保风格一致性
- 适当的数据增强
- 定期检查训练效果

### 3. 多LoRA组合使用

**权重分配：**
- 角色LoRA：0.6（保持角色特征）
- 风格LoRA：0.7（控制整体风格）
- InstantID：0.85（面部特征）
- IP-Adapter：0.5（场景细节）

**动态调整：**
- 根据场景类型调整权重
- 远景场景：降低角色LoRA，提高风格LoRA
- 特写场景：提高角色LoRA，降低风格LoRA

## 📁 目录结构

```
项目根目录/
├── processed/                  # 原动漫素材（已有）
│   ├── keyframes/              # 全局关键帧目录（5500+张）
│   │   ├── episode_170_clean-Scene-001_start.jpg
│   │   ├── episode_170_clean-Scene-001_middle.jpg
│   │   └── ...
│   ├── episode_142/
│   │   ├── keyframes/          # 各episode的关键帧
│   │   ├── scenes/             # 切分的场景视频
│   │   └── clean/              # 清理后的完整视频
│   └── ...
│
├── gen_video/
│   ├── reference_materials/
│   │   └── style_frames/       # 收集的风格参考图像（30-50张）
│   │       ├── style_ref_001.jpg
│   │       ├── style_ref_002.jpg
│   │       ├── ...
│   │       └── metadata.json    # 元数据
│   │
│   ├── models/
│   │   └── lora/
│   │       ├── hanli/          # 角色LoRA（已有）
│   │       └── anime_style/   # 风格LoRA（新增）
│   │           └── pytorch_lora_weights.safetensors
│   │
│   └── tools/
│       ├── collect_style_reference_frames.py  # 收集风格参考图像（从processed）
│       └── train_style_lora_sdxl.py          # 训练风格LoRA
```

## 🚀 快速开始

### 步骤1：收集风格参考图像

**从processed目录收集已有的关键帧：**

```bash
# 从processed目录收集30张代表性关键帧
python gen_video/tools/collect_style_reference_frames.py \
    --base-dir . \
    --output gen_video/reference_materials/style_frames \
    --num-frames 30 \
    --method diverse \
    --rename
```

**说明：**
- `--base-dir`: 项目根目录（包含processed目录）
- `--output`: 输出目录
- `--num-frames`: 需要选择的帧数（推荐30-50张）
- `--method`: 选择方法
  - `diverse`: 确保多样性（推荐），从不同场景选择
  - `balanced`: 平衡选择，每个episode平均分配
  - `random`: 随机选择
- `--rename`: 重命名为 style_ref_001.jpg, style_ref_002.jpg ...

**如果processed目录中没有关键帧，可以从视频提取：**

```bash
# 从视频中提取关键帧（如果需要）
python tools/extract_keyframes.py \
    --input processed/episode_*/clean/*.mp4 \
    --output reference_materials/style_frames/ \
    --num-frames 30 \
    --method scene_change
```

### 步骤2：训练风格LoRA

```bash
python scripts/train_style_lora_sdxl.py \
    --instance_data_dir reference_materials/style_frames/ \
    --output_dir models/lora/anime_style/ \
    --instance_prompt "anime style, xianxia animation style, 凡人修仙传 style"
```

### 步骤3：配置使用

在 `config.yaml` 中配置风格LoRA：

```yaml
image:
  lora:
    style_lora:
      enabled: true
      weights_path: models/lora/anime_style/pytorch_lora_weights.safetensors
      adapter_name: anime_style
      alpha: 0.7
```

### 步骤4：生成测试

```bash
python main.py --script test_scene.json --output test_style
```

## 💡 最佳实践

1. **参考图像选择**：
   - 选择高质量、风格统一的图像
   - 覆盖不同场景类型
   - 确保风格一致性

2. **LoRA训练**：
   - 使用足够的训练步数（800-1200）
   - 定期检查训练效果
   - 避免过拟合

3. **权重调节**：
   - 根据场景类型动态调整
   - 平衡角色和风格
   - 测试不同权重组合

4. **质量控制**：
   - 生成后检查风格一致性
   - 对比原动漫和新生成内容
   - 根据效果调整参数

## 🎯 总结

**推荐使用混合方案**：
- 风格LoRA：学习整体风格，保证统一性
- IP-Adapter Plus：补充场景细节，提高灵活性
- 角色LoRA + InstantID：保持角色一致性

这样既能学习整体风格，又能灵活生成符合解说场景的新内容！

