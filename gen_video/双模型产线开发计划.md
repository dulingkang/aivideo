# 双模型产线开发计划

## 📋 项目概述

建立**双模型产线结构**：
- **Pipeline A（高端产线）**：HunyuanVideo - 政府科普、企业广告、高端宣传片
- **Pipeline B（量产产线）**：CogVideoX-5B - 小说推文、短剧、日常运营

---

## 🎯 核心目标

### 短期目标（1-2周）
1. ✅ 完成CogVideoX-5B集成
2. ✅ 建立双模型路由系统
3. ✅ 实现模型自动选择逻辑
4. ✅ 完成短剧/推文自动机MVP

### 中期目标（1-3个月）
1. 优化短剧场景生成
2. 实现角色一致性（PuLID/InstantID）
3. 开发电商产品广告功能
4. 建立用户分级系统

### 长期目标（3-6个月）
1. 进入政府/城市科普赛道
2. 建立高端订阅服务
3. 实现多GPU节点并行
4. 完善商业化体系

---

## 📅 第一阶段：CogVideoX集成（优先级最高）

### 任务1：CogVideoX模型集成

**时间**：2-3天

**任务清单**：
- [ ] 检查CogVideoX模型下载状态
- [ ] 添加CogVideoX模型加载方法（`_load_cogvideox_model`）
- [ ] 实现CogVideoX视频生成方法（`_generate_video_cogvideox`）
- [ ] 添加CogVideoX配置到`config.yaml`
- [ ] 实现显存优化（CPU offload, attention slicing等）
- [ ] 测试CogVideoX基础生成功能

**代码位置**：
- `gen_video/video_generator.py` - 添加CogVideoX支持
- `gen_video/config.yaml` - 添加CogVideoX配置

**参考文档**：
- `gen_video/diffusers/src/diffusers/pipelines/cogvideo/pipeline_cogvideox_image2video.py`
- `gen_video/diffusers/tests/pipelines/cogvideo/test_cogvideox_image2video.py`

---

### 任务2：双模型路由系统

**时间**：1-2天

**任务清单**：
- [ ] 创建模型路由器（`ModelRouter`）
- [ ] 实现自动选择逻辑（根据场景类型、用户等级、显存情况）
- [ ] 添加模型切换API
- [ ] 实现模型预热和缓存机制
- [ ] 添加模型使用统计

**路由规则**：
```python
# 高端场景 → HunyuanVideo
if scene_type in ['government', 'enterprise', 'scientific', 'premium']:
    model = 'hunyuanvideo'
# 量产场景 → CogVideoX
elif scene_type in ['novel', 'drama', 'daily', 'social']:
    model = 'cogvideox'
# 默认 → CogVideoX（快速）
else:
    model = 'cogvideox'
```

**代码位置**：
- `gen_video/utils/model_router.py` - 新建模型路由器
- `gen_video/video_generator.py` - 集成路由逻辑

---

### 任务3：配置系统完善

**时间**：1天

**任务清单**：
- [ ] 在`config.yaml`中添加CogVideoX完整配置
- [ ] 添加模型选择配置（auto/hunyuanvideo/cogvideox）
- [ ] 添加场景类型到模型映射配置
- [ ] 添加用户等级到模型映射配置

**配置示例**：
```yaml
video:
  model_type: auto  # auto, hunyuanvideo, cogvideox
  model_routing:
    # 场景类型到模型的映射
    scene_type_mapping:
      government: hunyuanvideo
      enterprise: hunyuanvideo
      scientific: hunyuanvideo
      novel: cogvideox
      drama: cogvideox
      daily: cogvideox
      social: cogvideox
    # 用户等级到模型的映射
    user_tier_mapping:
      free: cogvideox
      basic: cogvideox
      professional: cogvideox
      enterprise: hunyuanvideo
      premium: hunyuanvideo
  
  # CogVideoX配置
  cogvideox:
    model_path: /path/to/cogvideox-5b-i2v
    num_frames: 81  # CogVideoX推荐帧数
    fps: 16  # CogVideoX推荐帧率
    width: 1360  # CogVideoX推荐分辨率
    height: 768
    num_inference_steps: 50
    enable_model_cpu_offload: true
    enable_tiling: true
```

---

## 📅 第二阶段：短剧/推文自动机（最赚钱）

### 任务4：短剧推文工作流

**时间**：3-5天

**任务清单**：
- [ ] 实现文本到分镜的LLM接口
- [ ] 实现分镜到场景JSON的转换
- [ ] 集成Flux生成底图
- [ ] 集成InstantID/PuLID锁脸
- [ ] 使用CogVideoX生成视频片段
- [ ] 实现Whisper字幕生成
- [ ] 实现视频拼接和合成

**工作流**：
```
输入文本 → LLM分镜 → 场景JSON → Flux生成图像 → InstantID锁脸 → 
CogVideoX生成视频 → Whisper字幕 → 视频合成 → 输出
```

**代码位置**：
- `gen_video/novel_to_video.py` - 新建短剧推文工作流
- `gen_video/utils/script_parser.py` - 文本到分镜解析器

---

### 任务5：角色一致性系统

**时间**：2-3天

**任务清单**：
- [ ] 集成InstantID或PuLID
- [ ] 实现角色脸锁定功能
- [ ] 实现角色库管理
- [ ] 实现多场景角色一致性

**代码位置**：
- `gen_video/utils/character_consistency.py` - 角色一致性工具
- `gen_video/config.yaml` - 添加角色配置

---

## 📅 第三阶段：用户分级和商业化

### 任务6：用户分级系统

**时间**：2-3天

**任务清单**：
- [ ] 实现用户等级管理
- [ ] 实现模型访问权限控制
- [ ] 实现使用量统计和限制
- [ ] 实现计费系统接口

**用户等级**：
```python
USER_TIERS = {
    'free': {
        'model': 'cogvideox',
        'max_duration': 6,
        'resolution': '720p',
        'daily_limit': 10
    },
    'professional': {
        'model': 'cogvideox',
        'max_duration': 12,
        'resolution': '1080p',
        'daily_limit': 100
    },
    'enterprise': {
        'model': 'hunyuanvideo',
        'max_duration': 30,
        'resolution': '1080p',
        'daily_limit': 1000
    },
    'premium': {
        'model': 'hunyuanvideo',
        'max_duration': 60,
        'resolution': '4k',
        'daily_limit': -1  # 无限制
    }
}
```

---

### 任务7：API接口完善

**时间**：2-3天

**任务清单**：
- [ ] 添加模型选择参数到视频生成API
- [ ] 添加用户等级验证
- [ ] 添加使用量统计
- [ ] 添加模型切换API
- [ ] 添加批量生成API（CogVideoX）

**API示例**：
```python
POST /api/v1/videos/generate
{
  "scenes": [...],
  "model_type": "auto",  # auto, hunyuanvideo, cogvideox
  "user_tier": "professional",
  "scene_type": "novel"  # novel, drama, government, etc.
}
```

---

## 🔧 技术实现细节

### CogVideoX集成代码结构

```python
# video_generator.py

def _load_cogvideox_model(self, model_path: str):
    """加载CogVideoX-5B模型"""
    from diffusers import CogVideoXImageToVideoPipeline
    
    self.cogvideox_pipeline = CogVideoXImageToVideoPipeline.from_pretrained(
        model_path,
        torch_dtype=torch.float16,
        low_cpu_mem_usage=True,
        device_map="cpu"
    )
    
    # 显存优化
    self.cogvideox_pipeline.enable_model_cpu_offload()
    self.cogvideox_pipeline.enable_tiling()
    
def _generate_video_cogvideox(self, image_path, output_path, prompt, num_frames, fps, scene):
    """使用CogVideoX生成视频"""
    # 实现CogVideoX生成逻辑
    pass

def generate_video(self, image_path, output_path, model_type=None, scene=None, ...):
    """生成视频（支持模型选择）"""
    # 根据model_type或自动选择模型
    if model_type is None:
        model_type = self._select_model(scene, user_tier)
    
    if model_type == 'cogvideox':
        return self._generate_video_cogvideox(...)
    elif model_type == 'hunyuanvideo':
        return self._generate_video_hunyuanvideo(...)
```

---

### 模型路由逻辑

```python
# utils/model_router.py

class ModelRouter:
    def select_model(self, scene: Dict, user_tier: str, available_memory: float) -> str:
        """根据场景、用户等级、显存情况选择模型"""
        
        # 1. 检查用户等级
        if user_tier in ['enterprise', 'premium']:
            # 高端用户可以使用HunyuanVideo
            scene_type = scene.get('type', 'general')
            if scene_type in ['government', 'enterprise', 'scientific']:
                if available_memory >= 20:  # 需要至少20GB显存
                    return 'hunyuanvideo'
        
        # 2. 检查场景类型
        scene_type = scene.get('type', 'general')
        if scene_type in ['novel', 'drama', 'daily', 'social']:
            return 'cogvideox'
        
        # 3. 默认使用CogVideoX（快速）
        return 'cogvideox'
```

---

## 📊 性能对比

| 指标 | HunyuanVideo | CogVideoX-5B |
|------|--------------|--------------|
| **生成速度** | 20-30分钟/30步 | 2-5分钟/50步 |
| **显存需求** | 20-30GB | 12-18GB |
| **分辨率** | 720p/1080p | 1360×768 |
| **帧数** | 24-120帧 | 81/161帧（推荐） |
| **质量** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **适用场景** | 高端、政府、企业 | 量产、短剧、推文 |
| **成本** | 高 | 低（约1/5） |

---

## 🚀 实施步骤

### 第1周：CogVideoX集成

**Day 1-2**：模型集成
- 检查模型下载状态
- 实现模型加载方法
- 实现基础生成功能
- 测试验证

**Day 3-4**：路由系统
- 创建模型路由器
- 实现自动选择逻辑
- 集成到video_generator
- 测试路由功能

**Day 5**：配置和优化
- 完善配置文件
- 优化显存使用
- 性能测试
- 文档更新

---

### 第2周：短剧推文自动机

**Day 1-2**：文本解析
- 实现LLM分镜接口
- 实现场景JSON转换
- 测试解析功能

**Day 3-4**：工作流集成
- 集成Flux图像生成
- 集成InstantID锁脸
- 集成CogVideoX视频生成
- 测试完整流程

**Day 5**：优化和测试
- 优化生成速度
- 测试批量生成
- 性能优化
- 文档完善

---

## 📝 配置文件更新

### config.yaml 新增配置

```yaml
video:
  # 模型选择：auto（自动）, hunyuanvideo, cogvideox
  model_type: auto
  
  # 模型路由配置
  model_routing:
    # 场景类型映射
    scene_type_mapping:
      government: hunyuanvideo
      enterprise: hunyuanvideo
      scientific: hunyuanvideo
      novel: cogvideox
      drama: cogvideox
      daily: cogvideox
      social: cogvideox
      general: cogvideox  # 默认
    
    # 用户等级映射
    user_tier_mapping:
      free: cogvideox
      basic: cogvideox
      professional: cogvideox
      enterprise: hunyuanvideo
      premium: hunyuanvideo
    
    # 显存阈值（GB）
    memory_threshold:
      hunyuanvideo: 20  # 需要至少20GB显存
      cogvideox: 12     # 需要至少12GB显存
  
  # CogVideoX配置
  cogvideox:
    model_path: /vepfs-dev/shawn/vid/fanren/gen_video/models/cogvideox-5b-i2v
    num_frames: 81  # 推荐帧数（81或161）
    fps: 16  # 推荐帧率
    width: 1360  # 推荐分辨率
    height: 768
    num_inference_steps: 50
    enable_model_cpu_offload: true
    enable_tiling: true
    max_memory_fraction: 0.5
```

---

## ✅ 检查清单

### CogVideoX集成
- [ ] 模型下载完成
- [ ] 模型加载方法实现
- [ ] 视频生成方法实现
- [ ] 显存优化完成
- [ ] 基础测试通过
- [ ] 性能测试完成

### 路由系统
- [ ] 模型路由器实现
- [ ] 自动选择逻辑完成
- [ ] 场景类型映射配置
- [ ] 用户等级映射配置
- [ ] 路由测试通过

### 短剧推文自动机
- [ ] 文本解析功能
- [ ] 分镜生成功能
- [ ] 图像生成集成
- [ ] 视频生成集成
- [ ] 字幕生成集成
- [ ] 完整流程测试

### 用户分级
- [ ] 用户等级管理
- [ ] 权限控制
- [ ] 使用量统计
- [ ] 计费接口

---

## 📚 参考资源

### CogVideoX文档
- `gen_video/diffusers/src/diffusers/pipelines/cogvideo/pipeline_cogvideox_image2video.py`
- `gen_video/diffusers/docs/source/en/api/pipelines/cogvideox.md`
- `gen_video/diffusers/tests/pipelines/cogvideo/test_cogvideox_image2video.py`

### 现有代码
- `gen_video/video_generator.py` - HunyuanVideo实现（参考）
- `gen_video/config.yaml` - 配置文件结构（参考）

---

## 🎯 成功标准

### 技术指标
- ✅ CogVideoX生成速度：<5分钟/81帧
- ✅ 显存占用：<18GB
- ✅ 模型切换时间：<30秒
- ✅ 批量生成：支持10+场景连续生成

### 业务指标
- ✅ 短剧推文自动机：单条视频<10分钟
- ✅ 成本：单条视频<0.1元
- ✅ 质量：用户满意度>80%

---

## 📞 下一步行动

1. **立即开始**：检查CogVideoX模型下载状态
2. **第1天**：实现CogVideoX基础集成
3. **第2-3天**：实现模型路由系统
4. **第4-5天**：测试和优化
5. **第2周**：开发短剧推文自动机

---

## 💡 关键提示

1. **优先完成CogVideoX集成**：这是建立量产产线的关键
2. **保持HunyuanVideo可用**：高端场景仍需要
3. **建立双模型体系**：不要只依赖一个模型
4. **快速迭代**：先实现MVP，再优化

---

**开始时间**：2025-12-11  
**预计完成**：2025-12-25（2周）  
**负责人**：开发团队

