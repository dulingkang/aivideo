# Scene_002和Scene_004生成问题修复总结

## 已修复的问题

### 1. ✅ **引擎选择错误（最关键）**
   - **问题**：系统选择了`flux-instantid`引擎，但这个引擎未实现，回退到普通Flux
   - **后果**：没有使用InstantID的参考图像（hanli_mid.png）和人脸特征，导致生成的人像不像韩立
   - **修复**：修改`model_selector.py`，CHARACTER任务类型返回`"instantid"`而不是`"flux-instantid"`
   - **位置**：`/vepfs-dev/shawn/vid/fanren/gen_video/model_selector.py` 第55行

### 2. ✅ **多人问题（两个头）**
   - **修复**：在`multiple_people_negative`中添加：
     - `(two heads:2.0)`
     - `(multiple heads:2.0)`
     - `(duplicate head:2.0)`
     - `(second head:2.0)`
   - **权重**：多人排除权重从1.8提高到2.0

### 3. ✅ **性别错误（变成女性）**
   - **修复**：排除女性权重从1.5提高到2.0
   - **添加排除项**：
     - `breasts, female breasts, (breasts:2.0)`
     - `long hair flowing, feminine hair, (feminine hair:2.0)`

### 4. ✅ **LoRA权重平衡**
   - **角色LoRA**: `1.0` → `0.75`（避免覆盖InstantID人脸特征）
   - **风格LoRA**: `0.65`（保持仙侠动漫风格）
   - **InstantID人脸权重**: `0.95`（高相似度）

### 5. ✅ **环境描述（沙漠场景）**
   - **逻辑**：prompt构建会包含：
     - `visual.environment`: "Gray-green desert floor"（权重1.4）
     - `prompt`: "Han Li lying on gray-green sand"（已包含在完整prompt中）
   - **从日志看**：prompt包含"(Han Li lying on gray-green sand:1.60)"，应该包含沙漠场景

### 6. ✅ **修仙风格**
   - **base_style_prompt**: "Chinese xianxia anime style, 3D rendered anime..."
   - **对于韩立角色**：会自动使用"凡人修仙传仙侠动漫风格"

## 当前配置

```yaml
image:
  engine: auto  # 现在会自动选择instantid（而不是flux-instantid）
  lora:
    alpha: 0.75  # 角色LoRA权重
    style_lora:
      alpha: 0.65  # 风格LoRA权重
  instantid:
    face_emb_scale: 0.95  # InstantID人脸权重
    face_image_path: /vepfs-dev/shawn/vid/fanren/gen_video/reference_image/hanli_mid.png
```

## 测试建议

重新生成scene_002和scene_004，应该会看到：
1. ✅ 使用InstantID引擎（而不是Flux）
2. ✅ 只有一个头（无多人问题）
3. ✅ 是男性韩立（无性别错误）
4. ✅ 场景在沙漠（环境描述应被包含）
5. ✅ 有仙侠动漫风格
6. ✅ 人脸更像韩立（使用了参考图像）

## 注意事项

如果场景仍然不对，检查生成日志：
- 是否显示"🎯 自动选择模型引擎: instantid"
- 是否显示"✓ 使用韩立的参考图"
- 最终prompt是否包含"gray-green sand"或"desert"
- negative prompt是否包含排除多人和女性的高权重项

