# HunyuanVideo 运动参数优化说明

## 🔍 问题分析

用户反馈：生成的视频中只有镜头运动，没有场景元素本身的运动（如瀑布流动、云彩飘动）。

**根本原因**：之前为减少SDXL视频跳动而调整的参数，可能抑制了HunyuanVideo的运动生成。

## ✅ 修复方案

### 1. 增加推理步数

**修改前**：
```yaml
num_inference_steps: 15  # 快速测试，质量不足
```

**修改后**：
```yaml
num_inference_steps: 25  # 推荐用于生产（质量好，速度快，运动自然）
```

**理由**：
- 15步对于蒸馏模型来说可能太少，不足以生成自然的运动
- 25步是蒸馏模型的推荐值，既能保证质量又能生成自然的运动
- 步数不足可能导致运动不明显或不自然

### 2. 提高引导尺度

**修改前**：
```yaml
guidance_scale: 7.0  # 降低可能改善色彩准确性
```

**修改后**：
```yaml
guidance_scale: 7.5  # 提高以增强运动效果
```

**理由**：
- guidance_scale过低可能抑制运动生成
- 7.5是平衡点，既能保证质量又能促进运动
- 范围建议：6.0-8.0

### 3. 优化负面提示词（关键修复）

**修改前**（为减少SDXL跳动而添加）：
```
"low quality, blurry, distorted, deformed, bad anatomy, bad hands, text, watermark, 
flickering, jittery, unstable, sudden movement, abrupt changes"
```

**修改后**（鼓励运动，避免静态）：
```
"low quality, blurry, distorted, deformed, bad anatomy, bad hands, text, watermark, 
static, frozen, no motion, still image"
```

**关键变化**：
- ❌ 移除：`flickering, jittery, unstable, sudden movement, abrupt changes`
  - 这些词是为了减少SDXL跳动而添加的
  - 但可能抑制了HunyuanVideo的运动生成
- ✅ 添加：`static, frozen, no motion, still image`
  - 明确鼓励运动，避免静态画面
  - 不抑制自然运动，只避免跳动

### 4. 参数对比

| 参数 | SDXL/SVD（减少跳动） | HunyuanVideo（鼓励运动） | 说明 |
|------|---------------------|------------------------|------|
| `num_inference_steps` | 40-50 | 25（蒸馏）或30-40（标准） | HunyuanVideo需要足够步数生成运动 |
| `guidance_scale` | 6.0 | 7.5 | 提高以增强运动效果 |
| `negative_prompt` | `flickering, jittery, unstable, sudden movement` | `static, frozen, no motion` | 鼓励运动而非抑制运动 |
| `motion_bucket_id` | 1.5（减少跳动） | N/A | HunyuanVideo不使用此参数 |
| `noise_aug_strength` | 0.00025（减少跳动） | N/A | HunyuanVideo不使用此参数 |

## 📊 参数建议

### 蒸馏版本（当前使用）
```yaml
num_inference_steps: 25  # 推荐，平衡质量和速度
guidance_scale: 7.5      # 推荐，促进运动生成
negative_prompt: "low quality, blurry, distorted, deformed, bad anatomy, bad hands, text, watermark, static, frozen, no motion, still image"
```

### 标准版本（如果需要更高质量）
```yaml
num_inference_steps: 30-40  # 标准版本推荐
guidance_scale: 7.5         # 同样推荐
negative_prompt: "low quality, blurry, distorted, deformed, bad anatomy, bad hands, text, watermark, static, frozen, no motion, still image"
```

## 🎯 关键改进

1. **增加推理步数**：从15步提高到25步，确保运动质量
2. **提高引导尺度**：从7.0提高到7.5，增强运动效果
3. **优化负面提示词**：移除抑制运动的词，添加鼓励运动的词

## ⚠️ 注意事项

1. **不要混用参数**：SDXL的参数（如`motion_bucket_id`、`noise_aug_strength`）不适用于HunyuanVideo
2. **负面提示词很重要**：过于严格的负面提示词可能抑制运动生成
3. **步数要足够**：步数不足可能导致运动不明显或不自然

## 📝 总结

- ✅ 增加推理步数到25步（蒸馏版本推荐）
- ✅ 提高引导尺度到7.5（促进运动生成）
- ✅ 优化负面提示词（移除抑制运动的词，添加鼓励运动的词）
- ✅ 确保参数适合HunyuanVideo，而不是SDXL

现在HunyuanVideo应该能够生成自然的运动，而不仅仅是镜头运动！



