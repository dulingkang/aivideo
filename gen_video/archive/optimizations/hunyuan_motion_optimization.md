# HunyuanVideo 动作幅度优化

## ✅ 已优化的参数

### 1. guidance_scale（最关键）
- **原值**：7.5
- **新值**：8.5
- **作用**：提高引导强度，增强运动幅度和动作强度
- **建议范围**：8.0-9.0（平衡质量和运动幅度）

### 2. num_inference_steps
- **原值**：20
- **新值**：25
- **作用**：增加推理步数，提高运动质量和自然度
- **建议范围**：25-30（蒸馏版本）

## 📝 其他优化建议

### 1. 在 Prompt 中添加运动描述
在视频生成的 prompt 中明确描述运动：
- "gentle motion" → "smooth motion" 或 "dynamic movement"
- 添加具体的动作描述，如 "slight movement", "natural motion", "flowing movement"

### 2. 如果仍然不够，可以尝试：
```yaml
guidance_scale: 9.0  # 进一步提高（注意：可能影响质量）
num_inference_steps: 30  # 进一步提高（增加生成时间）
```

### 3. 负面 Prompt 优化
确保负面 prompt 不抑制运动：
```yaml
negative_prompt: "low quality, blurry, distorted, deformed, bad anatomy, bad hands, text, watermark, static, frozen, still image"
# 注意：移除了 "no motion" 等可能抑制运动的词
```

## ⚠️ 注意事项

1. **guidance_scale 过高**（>9.5）可能导致：
   - 过度运动
   - 画面不稳定
   - 质量下降

2. **num_inference_steps 过高**（>30）会：
   - 增加生成时间
   - 对动作幅度的提升有限

3. **平衡建议**：
   - 先尝试 `guidance_scale: 8.5, num_inference_steps: 25`（当前配置）
   - 如果还不够，逐步提高到 `guidance_scale: 9.0, num_inference_steps: 30`
   - 如果过度运动，降低到 `guidance_scale: 8.0, num_inference_steps: 25`
